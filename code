from gtts import gTTS                                

from playsound import playsound

audio='speech.mp3'
language='en'
sp=gTTS(text=str(input("text please: ")),lang=language,fast=True)

sp.save(audio)
playsound(audio)
